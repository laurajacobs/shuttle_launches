# shuttle_launches
A brief thrown together app with one quick api call to get next five launches

Steps to run:
1.) Once navigated to project folder run ```npm install``` in a terminal
2.) Then run ```npm start```. (This should spin up new tab on your browser running a version on local host.)
